# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/*
Dockerfile.cross

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# editor and IDE paraphernalia
.idea
.vscode
*.swp
*.swo
*~

# Kubeconfig might contain secrets
*.kubeconfig

vendor

yaml

testoutput

# Vendored Helm subchart archives (reproducible via `helm dependency build`
# from Chart.lock). Chart.lock itself is committed.
charts/*/charts/*.tgz